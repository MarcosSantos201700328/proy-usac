2005-08-25  Kreig Iron  <kreig@localhost.localdomain>

	* proy1.pas:
	Cambiada el procedimiento Punto Medio por funcion(PuntoMedio()), agregada fuciones para desviacion REspecto a la Media Supuesta y agregado procedimiento para el Total de la desviacion;

	* proy1.pas:
	Implementado procedimiento PuntoMedio que genera valor de puntomedio en el array PM[] para cada intervalo

	* proy1.pas:
	Agregada la funcion Frecuencia, que ordena los datos en cantidades especificadas en el vector 'frec[]' respecto el intervalo al que pertenecen

2005-08-24  Kreig Iron  <kreig@localhost.localdomain>

	* proy1.pas:
	Agregado el parser para Intervalos, acepta espacios entre el signo '<', y guarda los datos en los arrays intervd e intervi

	* proy1.pas:
	Cambiada la funcion EsNum por el procedimiento mas general parse.
	Ya lee datos en el bloque DATOS y los almacena en el array DATOS

	* proy1.pas:
	Agregada la funcion isnum, probarla para su uso en la linea 105

2005-08-23  Kreig Iron  <kreig@localhost.localdomain>

	* proy1.pas:
	Armado el esqueleto del las acciones principales a ser tomadas

	* proy1.pas: Agregado reconocimiento de comandos en inputfile()

	* proy1.pas: New file.
